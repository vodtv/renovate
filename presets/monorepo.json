{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Node.js Application 后端应用预设",
  "extends": ["./base"],
  "rangeStrategy": "pin",
  "packageRules": [
    {
      "matchUpdateTypes": ["patch", "minor"],
      "automerge": true,
      "requiredStatusChecks": ["build", "lint", "test"]
    },
    {
      "matchUpdateTypes": ["major"],
      "automerge": false
    }
  ]
}
